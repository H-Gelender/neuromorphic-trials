[project]
name = "recherche-agi"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai-slim[openai]",
    "torch>=2.13",
    "torchvision>=0.28",
    "jupyterlab",
]

[project.scripts]
recherche-agi = "recherche_agi:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
